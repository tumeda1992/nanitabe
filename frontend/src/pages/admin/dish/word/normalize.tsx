import React from 'react';

export const getServerSideProps = async () => {
  return { props: {} };
};

export default () => {
  return (
    <div>
      <h1>単語正規化ページ</h1>
      {/* ここにコンテンツを追加 */}
    </div>
  );
};
